node_modules/
.DS_Store
*.log
*.tsbuildinfo

.env
.env.*
!.env.example

.next/
dist/
build/
.expo/
.expo-shared/

# Carpetas nativas que genera `expo prebuild` (lo corre `expo run:ios`).
# Se regeneran desde app.json, así que versionarlas es tener la configuración
# dos veces y que una quede vieja.
apps/mobile/ios/
apps/mobile/android/

coverage/
.vercel

# Playwright MCP browser session artifacts
.playwright-mcp/

# Manifiesto del seed de datos de prueba: apunta a una base concreta.
apps/admin/scripts/.datos-prueba.json
apps/admin/scripts/.visita-con-fotos.json

# Claude Code local files (ignore everything under .claude EXCEPT the committed docs)
.claude/*
!.claude/docs/
